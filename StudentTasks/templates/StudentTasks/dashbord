{% extends 'Tasks/base.html' %}
{% load static %}
{% block content %}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contract Dashboard - Task Management</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
      
    
    <style>
      
    
    </style>

    <!-- Header section -->
    
    <h2>{{ contract.title }}</h2>
        <p>
            <strong>Duration:</strong> {{ contract.start_date }} to {{ contract.end_date }}|
            <strong>Progress:</strong> {{ contract.progress|floatformat:1 }}%
        </p>
        <hr>
</header>

    <!-- Navigation -->
    <nav>
       <a href="{% url 'StudentTasks:contract_list' %}" >My Contracts</a> |

        <a href="{% url 'StudentTasks:contract_create' %}">Create a new contract</a>| 
        <hr>
    </nav>
         <div class="progress" style="height: 12px;"><strong>
                        <div class="progress-bar" role="progressbar" style="width:20px';"
                             aria-valuenow="" aria-valuemin="0" aria-valuemax="100"></div>
                             
                    </div>
        <body>
            <h1>Task Management System</h1>
            {% if messages %}
            <ul>
                {% for message in messages %}
                <li>{{ message }}</li>
                {% endfor %}
            </ul>
            {% endif %}
            

                <ul class="navbar-nav">
                                {% if user.is_authenticated %}
                                    <li class="nav-item">
                                        <span class="nav-link">Welcome, {{ user.username }}</span>
                                    
                                {% endif %}
                            </ul>
                        </div>
                    </div>
                </nav>

                <main class="container mt-4">
                  
                    <div class="jumbotron">
                        <p class="lead">Manage your training contracts and daily tasks efficiently.</p>
                        <hr class="my-4">
                        <p>Get started by creating a new contract or viewing your existing ones.</p>
                        <br>
                    </div>
                        <br>
                        <br><br>
                        <div class="card">
                        <h5>Student Home</h5>
                        <p>Manage your Tasks</p>
                        {% if contract %}
                            <a href="{% url 'StudentTasks:weeklytask_list' contract.id %}" class="btn">tasks list</a>
                        {% endif %}                            
                    </div>
                        <br>
                        <br>
                        <br>
                    <div class="card">
                        <h5>Student Home</h5>
                        <p>Manage your Tasks</p>
                        <a href="{% url 'StudentTasks:student_tasks_home' %}" class="btn">Home</a>
                    </div>
                        <br>
                        <br>
                        <br>
                    <div class="card">
                        <h5>Weekly Task</h5>
                        <p>Add your weekly Tasks</p>
                        {% if contract %}
                            <a href="{% url 'StudentTasks:weeklytask_create' contract.id %}" class="btn">Add Weekly Task</a>
                        {% endif %}
                    </div>
        </main>
     </html>

      <!-- Completed Tasks -->
    <div class="col-md-4 mb-4">
      <h5>Completed Tasks</h5>
      <ul class="list-group">
        {% for task in completed_tasks %}
        <li class="list-group-item">
          {{ task.title }} - completed on {{ task.datecomplited|date:"M d, Y" }}
        </li>
        {% empty %}
        <li class="list-group-item text-muted">No completed tasks yet.</li>
        {% endfor %}
      </ul>
    </div>
  </div>